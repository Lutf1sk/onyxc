// A comment

def usz :: u64,
	isz :: i64;

let print :: void(u8* str, usz len) {
	syscall 1, 1, str, len;
}

let add :: i64(i64 a, i64 b) {
	return a + b;
}

def struc :: struct {usz i; isz j;}

let nl = '\n';

let main :: i32() {
	let a = 1, b = 2;

	print(&b, 1);
	print(&b, 1);

	return a + b;
}

