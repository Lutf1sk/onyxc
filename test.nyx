
def usz :: u64,
	isz :: i64;

let printu :: void(u64 n) {
	while n {
		u8 c = n % u8: 10 + '0';
		n = n / usz: 10;
		syscall 1, 1, &c, 1;
	}
}

let main :: i32() {
	printu(u64: 1234);
	u8 nl = '\n';
	syscall 1, 1, &nl, 1;
	return 0;
}

