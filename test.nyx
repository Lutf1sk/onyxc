
def usz :: u64,
	isz :: i64;

let print :: void(u8[] str) {
	syscall 1, 1, str.data, str.count;
}

let print_m :: void(u8[][] strs) {
 	usz i = usz: 0;
	while i < strs.count {
		print(strs[i]);
 		++i;
 	}
}

let main :: u64() {
	print_m({ "Skjut mig\n", "Skjut mig 2\n" });
	return usz: 0;
}

