// A comment

def usz :: u64,
	isz :: i64;

let print :: void(u8* str, usz len) {
	syscall 1, 1, str, len;
}

let add :: i64(i64 a, i64 b) {
	return a + b;
}

def struc :: struct {
	usz i;
	isz j;
}

let nl = '\n';

let main :: i32() {
	usz a = u32: 1, b = u32: 2;

	print("Test\n", usz: 5, 1);
	print(&nl, usz: 1);

	return a + b;
}

