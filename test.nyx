
def type :: struct {

};

let main :: void*() void() {
	let a = 0;
	let b = &a;
	let c = u32* b;
	let d = type *c;

	type impl;

	{
		i32 e = 0;
		i32* e_ptr = &e;
		let i = e_ptr[0];
	}

	return d;
};

