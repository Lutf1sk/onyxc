
def usz :: u64,
	isz :: i64;

let print :: void(u8* str, usz len) {
	struct { u32 i; i64 j; } var;

	var.j = 1;
	syscall 1, 1, str, len;
}

let main :: i32() {
	print("Test\n", usz: 5);

	return 0;
}

