import "std/io.nyx";

Asdf :: struct {
	a : isz;
	b : isz;
};

main :: i32(argc i32, argv u8**) {

	fdsa :: Asdf { 1, 2 };

	for {
		print("printed once\n");
		break;
	}

	for i: 2..7 {
		print("printed five times\n");
		continue;
	}

	exit(0);
};
