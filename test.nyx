
def usz :: u64,
	isz :: i64;

def astruct :: struct {
	i32 a;
}

let print :: void(u8* str, usz len) {
	syscall 1, 1, str, len;
}

let main :: i32() {
	astruct t;
	print("Test\n", usz: 5);
	return 0;
}

