
def usz :: u64,
	isz :: i64;

let print :: void(u8[] str) {
	syscall 1, 1, str.data, str.count;
}

let print_m :: void(u8[][] strs) {
 	usz i = usz: 0;
	while (i < strs.count) {
		print(strs[i]);
 		++i;
 	}
}

let main :: u64() {
	u8[] str = "ASDF\n";
	u8[] str2 = "ASDF2\n";

	u8[][2] arr;
	arr[0] = str;
	arr[1] = str2;
	print_m(arr);

	return usz: 0;
}

