// A comment

def usz :: u64
def isz :: i64

let print :: void(u8* str, usz len) {
	syscall 1, 1, str, len;
}

let add :: i64(i64 a, i64 b) {
	return a + b;
}

def struc :: struct {usz i; usz j;}

let main :: i32() {
	print("asdf\n", 5);
	return 0;
}

