import "io.nyx", "elf.nyx";

let main :: u64() {
	isz fd;
	if &fd->file_openr("a.out") {
		print("Failed to open 'a.out'\n");
		exit(1);
	}
	fd->file_close();

	for u32 i..0x10 {
		print_i64(i);
		print(": 0x");
		print_h64(i);
		print("\n");
	}

// 	void(){
// 		print("asdf\n");
// 		let a = "ASDFASDFASDF";
// 	}();

	exit(0);
}

